

1. 기본 모델 (0.3493) ✅
설정:
- 특성: 14개 (value 기반만)
  - 후행: b_t, b_t_1, b_t_2, b_ma3, b_change
  - 선행: a_t_lag, a_t_lag_1, a_ma3, a_change, ab_value_ratio
  - 관계: max_corr, best_lag, consistency, corr_stability
- 공행성쌍: 3,000개
- 상관계수 임계값: 0.30
- Lag 범위: 1~7개월
- 모델: XGBoost (단일)
- 하이퍼파라미터: n_estimators=150, max_depth=5, lr=0.08

장점:
- 단순하고 안정적
- 과적합 없음
- 검증된 성능

2. 기본 모델 재현 (0.337) ⚠️
설정: 기본 모델과 동일

문제점:
- 0.3493 → 0.337로 하락 (1.2% 감소)
- 재현성 문제 발생

추정 원인:
1. 데이터 변경 가능성
2. 랜덤 시드 차이
3. 라이브러리 버전 차이
4. 데이터 전처리 방식 차이

3. 개선 모델 v1 (0.3064) ❌
설정:
- 특성: 16개 (b_ma6, b_ma12 추가)
- 공행성쌍: 4,000개
- 상관계수 임계값: 0.27 (완화)
- 모델: XGBoost + LightGBM (60:40)
- 일관성 가중치: 1.3 + 추가 가중치

문제점:
1. 상관계수 임계값을 너무 낮춤 (0.27) → 노이즈 쌍 다수 포함
2. 쌍이 너무 많음 (4,000개) → 품질 저하
3. 특성 추가가 오히려 과적합 → b_ma6, b_ma12가 노이즈로 작용
4. 일관성 가중치가 너무 복잡 → 과최적화
5. 점수 계산 복잡화 → 상관계수 × 일관성 × (1 + 안정성)

결과:
- 0.337 → 0.3064로 대폭 하락 (9.1% 감소)
- 심각한 과적합 발생

핵심 교훈

성공 요인:
1. 단순함이 최고 - 복잡한 모델 < 단순한 모델
2. 품질 > 양 - 쌍 수보다 쌍의 품질이 중요
3. 검증된 특성만 - 새로운 특성 추가 시 신중
4. 보수적인 임계값 - 상관계수 0.30이 적절

실패 요인:
1. 과도한 최적화 - 임계값 낮춤, 쌍 증가, 특성 추가 동시 적용
2. 복잡한 가중치 - 일관성 가중치 1.3 + 조건부 추가
3. 특성 과다 - b_ma6, b_ma12 추가가 역효과


결론

현재 상황:
- 기본 모델: 0.337 (재현)
- 개선 시도: 0.3064 (실패)

권장 사항:
1. 전략 A 시도 (보수적 개선)
2. 실패 시 전략 B (앙상블만)
3. 최후 전략 C (품질 극대화)

핵심:
- 한 번에 하나씩만 변경
- 과도한 최적화 금지
- 단순함 유지
